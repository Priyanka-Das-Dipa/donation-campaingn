import React from 'react';


const Donation = ({donationCards}) => {
    console.log(donationCards)
    return (
        <div>
            {
                
            }
        </div>
    );
};

export default Donation;